//Take four parameters. Multiply the four numbers and then return the result

function multiply(n1, n2, n3, n4) {
  let result = n1 * n2 * n3 * n4;
  return result;
}

let total = multiply(1, 2, 3, 4);
console.log(total);
